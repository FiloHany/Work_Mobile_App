import 'package:flutter_test/flutter_test.dart';

// Smoke test placeholder — real tests are in test/engine/.
// WorkApp requires Supabase + Firebase initialisation; integration tests
// cover full app flows. This file satisfies the default test runner.
void main() {
  test('placeholder — see test/engine/ for business-logic tests', () {
    expect(true, isTrue);
  });
}
